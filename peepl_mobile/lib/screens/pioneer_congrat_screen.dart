import 'package:flutter/material.dart';
import '../widgets/peepl_ported_screen_shell.dart';

class PioneerCongratScreen extends StatelessWidget {
  const PioneerCongratScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return PeeplPortedScreenShell(
      title: 'Pioneer congrats',
      description: 'RN: milestone / pioneer achievement screen.',
    );
  }
}
