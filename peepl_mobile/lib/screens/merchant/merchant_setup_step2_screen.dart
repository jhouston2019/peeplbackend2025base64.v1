import 'package:flutter/material.dart';
import '../../widgets/peepl_ported_screen_shell.dart';

class MerchantSetupStep2Screen extends StatelessWidget {
  const MerchantSetupStep2Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return PeeplPortedScreenShell(
      title: 'Merchant setup (2)',
      description: 'RN: merchant registration step 2.',
    );
  }
}
